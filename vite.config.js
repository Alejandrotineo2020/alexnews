// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // Cambia 'noticias-tech' por el nombre exacto de tu repositorio en GitHub
  base: '/noticias-tech/',
  build: {
    outDir: 'dist',
  },
});
